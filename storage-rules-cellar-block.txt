rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Eats photos — Phil/Trinity only
    match /eats/{allPaths=**} {
      allow read, write: if request.auth != null && (
        request.auth.uid == 'CuJigrzCbRfFFsM7uqe0mix46xH3' ||
        request.auth.uid == 'g4stabGPKiWAE23NTw6oUsqawWf1'
      );
    }
    // FoodSwipe photos — Phil/Trinity only
    match /foodswipe/{allPaths=**} {
      allow read, write: if request.auth != null && (
        request.auth.uid == 'CuJigrzCbRfFFsM7uqe0mix46xH3' ||
        request.auth.uid == 'g4stabGPKiWAE23NTw6oUsqawWf1'
      );
    }
    // House data (Command Center) — Phil/Trinity only
    match /house_data/{allPaths=**} {
      allow read, write: if request.auth != null && (
        request.auth.uid == 'CuJigrzCbRfFFsM7uqe0mix46xH3' ||
        request.auth.uid == 'g4stabGPKiWAE23NTw6oUsqawWf1'
      );
    }
    // AutoFlag — any signed-in user (admins + installers)
    match /autoflag/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
    // Command Center (AI photo uploads, etc.) — Phil/Trinity only
    match /command_center/{allPaths=**} {
      allow read, write: if request.auth != null && (
        request.auth.uid == 'CuJigrzCbRfFFsM7uqe0mix46xH3' ||
        request.auth.uid == 'g4stabGPKiWAE23NTw6oUsqawWf1'
      );
    }
    // Cellar — wine label photos; any signed-in roster member
    // (the app uploads to cellar/bottles/{id}/… — keep this path name in sync)
    match /cellar/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
    // Default deny — anything not explicitly allowed above is rejected
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
